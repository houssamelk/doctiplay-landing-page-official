{
  "name": "doctiplay-landing-page",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "install:ppt": "cd ppt-deck && npm install",
    "build:ppt": "cd ppt-deck && npm run build",
    "predeploy": "npm run install:ppt && npm run build:ppt && npm run build && mkdir -p dist/pitch && cp -r ppt-deck/dist/* dist/pitch/",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "@google/genai": "latest",
    "lucide-react": "^0.561.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "gh-pages": "^6.3.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}